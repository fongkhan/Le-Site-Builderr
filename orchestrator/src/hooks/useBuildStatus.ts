// Conservé pour compatibilité des imports existants : le hook vit désormais dans
// le contexte partagé (un seul poller pour toute l'app).
export { useBuildStatus } from '../state/BuildStatusContext';
